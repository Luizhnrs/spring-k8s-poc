apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: tickets-postgres-pvc
  labels:
    app: tickets-postgres
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: standard