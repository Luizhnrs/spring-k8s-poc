apiVersion: v1
kind: Service
metadata:
  name: tickets-postgres-service
  labels:
    app: tickets-postgres
spec:
  type: ClusterIP
  ports:
    - port: 5432
      targetPort: 5432
      protocol: TCP
      name: postgres
  selector:
    app: tickets-postgres