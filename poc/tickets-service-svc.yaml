apiVersion: v1
kind: Service
metadata:
  name: tickets-service
  labels:
    app: tickets-service
spec:
  type: ClusterIP
  ports:
    - port: 8081
      targetPort: 8081
      protocol: TCP
      name: http
  selector:
    app: tickets-service